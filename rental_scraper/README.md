# Rental Website Scraper

A simple web scraper to extract rental listings from a website.

## Installation

1. Install dependencies: `pip install -r requirements.txt`

## Usage

Run `python app.py` and enter the URL of the rental website when prompted.

Note: Adjust the HTML selectors in the code based on the specific website's structure. Always check the website's terms of service before scraping.