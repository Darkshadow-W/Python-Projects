# YouTube Video Downloader

A simple Python script to download YouTube videos.

## Installation

1. Install dependencies: `pip install -r requirements.txt`

## Usage

Run `python app.py` and enter the YouTube URL when prompted.